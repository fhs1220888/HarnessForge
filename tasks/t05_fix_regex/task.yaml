prompt: |
  The validate_email function in validators.py has a buggy regex — some valid
  emails are rejected and some invalid ones accepted. Fix it so the tests pass.
  Do not modify the tests.
setup: "pip install -q pytest"
check: "cd /workspace && python -m pytest tests/ -q"
tags: [bugfix, python, regex]
timeout_s: 180
